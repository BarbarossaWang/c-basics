/* Write a program to determine the ranges of char, short, int and long variables, both signed and unsigned, by printing appropriate values form standard headers and by direct computation.
 * Harder if you compute them: determine the ranges of various floating-point types.
 * */
#include <stdio.h>
#include <limits.h>
#include <float.h>

void print_std_h(void);
void direct_compute(void);

int main()
{
  return 0;
}

void print_std_h()
{
}

void direct_compute()
{
}