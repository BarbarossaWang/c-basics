/* Write a program entab that replaces strings of blanks by the minimum number of tabs and blanks to achieve the same spacing. Using the same tab stops for detab.
 * When either a tab or a single blank would suffice to reach a tab stop, which be given preferece? */
#include <stdio.h>

int main()
{
  return 0;
}